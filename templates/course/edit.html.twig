{% extends 'base.html.twig' %}

{% block title %}Edit Course{% endblock %}

{% block body %}
    <div class="container-fluid ">
        <h1>{{ course.title }}</h1>
        <div class="container-fluid border-bottom border-secondary">
            {{ include('course/_form.html.twig', {'button_label': 'Сохранить'}) }}
            <a class="btn btn-secondary  mt-2 mb-2" href="{{ path('app_course_index') }}">Вернуться к списку</a>
        </div>
        <div class="container-fluid">
            <div class="mt-2">
                {{ include('course/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}